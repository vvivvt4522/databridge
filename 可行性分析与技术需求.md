# 三端数据互通工具 — 可行性分析与技术需求

> 目标：在 Windows PC、手机、iPad 三端之间互传文件（图片/文档）和文本/链接，不依赖微信/QQ，隐私、快速、随时可用。

---

## 1. 可行性结论

**完全可行。** 这条赛道已被多个开源项目验证（LocalSend、PairDrop/Snapdrop、KDE Connect），没有不可逾越的技术障碍，非常适合作为个人项目。真正的工作量和风险集中在三处：

| 风险点 | 说明 | 对策 |
|---|---|---|
| **iPadOS/iOS 沙盒限制** | 无法常驻后台、无法自装原生应用（除非 $99/年开发者账号 + 上架审核） | 不做原生 App，用浏览器 PWA + 系统快捷指令（Shortcuts）+ 分享菜单绕开 |
| **Android 10+ 剪贴板限制** | 后台应用被禁止读取剪贴板 | 放弃"全自动剪贴板同步"，改为**手动触发**："一键发送剪贴板"按钮 / 分享菜单 / 快捷磁贴 |
| **跨网络场景** | 局域网直连只在同一 WiFi 下可用 | MVP 只做局域网；跨网络用 Tailscale 免费组网，或 VPS 中继 |

---

## 2. 三种架构方案

### 方案 A：局域网直连（推荐起步）
PC 上跑一个轻量 HTTP 服务，手机/iPad 浏览器**扫码**访问同一个网页。

- 设备发现：mDNS（Bonjour/Avahi）自动发现 + 扫码兜底
- 传输：HTTP 分块上传/下载（文件）+ WebSocket（文本/控制消息）
- 优点：零成本、速度快（受 WiFi 带宽限制，通常 20–80 MB/s）、数据不出内网、隐私最好
- 缺点：必须在同一局域网；PC 关机时"手机→PC"方向不可用

### 方案 B：自建中继服务器
租一台 VPS（或家里 NAS + 内网穿透），三端都连它。

- 优点：随时随地可用，不依赖 PC 开机
- 缺点：服务器带宽/流量成本、隐私需靠端到端加密保障、多一个维护点

### 方案 C：混合架构（终极形态）
局域网优先直连，发现不了同一网络时回落到中继服务器。LocalSend 即此思路。

### 网络层的捷径：Tailscale
个人免费的零配置组网工具（三端都有客户端）。装上之后，手机在外面 4G/5G 下也和家里 PC"处于同一局域网"，**方案 A 的代码一行不用改就能跨网络使用**。如果接受装 Tailscale，可以砍掉整个中继服务器的开发量。

---

## 3. 推荐形态：PC 常驻服务 + 三端网页（PWA）

**不推荐三端都做原生 App**，理由：

- iOS/iPadOS：开发者账号 $99/年、上架审核周期、无法自由分发，且后台限制依旧存在
- Android：APK 虽自由，但要单独维护一套代码
- 三端原生 = 3 倍工作量，个人项目不可持续

**推荐形态：**

| 端 | 形态 |
|---|---|
| Windows PC | 单二进制服务（Go）或 Tauri/Electron 托盘程序；开机自启、托盘图标、原生通知；内嵌静态网页 |
| 手机（Android/iPhone） | 浏览器打开 PC 上的页面 → "添加到主屏幕"（PWA），首次用二维码 + token 配对 |
| iPad | 同上；进阶用**快捷指令**实现分享面板发送、剪贴板一键发送 |

这个形态把"客户端"收敛为一个网页，三端体验天然一致，升级只需改服务端。

---

## 4. 核心功能模块拆解

### 4.1 配对与安全
- 首次连接：PC 屏幕显示二维码（内含 `http://<局域网IP>:<端口>/?token=xxx`）
- token 持久化在浏览器 localStorage，之后自动登录
- 可选升级：自签证书上 HTTPS（mkcert），或局域网明文 + token（家用场景可接受）

### 4.2 文件传输（解决"图片手机传电脑"）
- 浏览器 File API 选择/拖拽 → 分块 POST 上传（支持大文件、断点续传、进度条）
- PC 接收：保存到指定目录 + Windows 弹通知（可配"自动接收"）
- PC 发送：网页拖拽上传，进阶做右键菜单"发送到设备"
- 手机/iPad 接收：网页里点一下直接下载（图片可存入相册）
- 多文件批量、图片原图传输（不压缩，这是相对微信的核心优势）

### 4.3 文本 / 链接同步（解决"电脑复制链接给手机"）
- WebSocket 双向消息通道，设备在线状态实时可见
- PC 端：托盘菜单/快捷键"推送当前剪贴板"到手机（Windows 剪贴板监听用 `AddClipboardFormatListener`，Electron 用 clipboard 模块）
- 手机端：打开页面看到最新推送 → 一键复制按钮
- 注意：因 Android 10+ 和 iOS 的后台限制，**不做无人值守自动同步**，改为"发送方主动推、接收方打开即见"的半自动模式，体验已经够好

### 4.4 设备管理
- 设备列表（在线/离线、命名、图标）
- 支持多于 3 台设备并发连接
- 传输历史记录 + 过期自动清理

---

## 5. 各平台限制清单（踩坑点）

| 平台 | 限制 | 影响 / 对策 |
|---|---|---|
| iOS/iPadOS | Safari 网页无后台常驻，切后台标签可能被杀 | 接收采用"打开页面即拉取最新内容"；iOS 16.4+ 添加到主屏幕的 PWA 支持 Web Push，可实现新消息提醒 |
| iOS 14+ | 读取剪贴板会弹系统提示 | 可接受的交互成本，无需规避 |
| Android 10+ | 后台应用禁止读剪贴板 | 手动触发：页面按钮 / 系统分享菜单 / 快捷磁贴 |
| Windows | 首次监听端口触发防火墙弹窗 | 安装/首启时引导放行，或自动添加防火墙规则 |
| 局域网 HTTP | Service Worker 等 PWA 完整能力要求 HTTPS（localhost 除外） | V0.1 用纯 HTTP + 书签即可用；需要离线缓存/推送时再上 mkcert 自签证书 |
| iPad 相册 | 网页下载图片需手动"存储到照片" | 可接受；快捷指令可自动化的部分后续再做 |

---

## 6. 技术栈建议

**路线一：最快出 MVP（纯 Web，无桌面壳）**
- 服务端：Node.js（Fastify + ws）或 Go（Gin/Fiber + gorilla/websocket），静态托管前端
- PC 常驻：任务计划程序 / nssm 注册为后台服务
- 优点：一两周内可用；后续可无缝升级为路线二

**路线二：产品化桌面端**
- Tauri 2.x（Rust + 系统 WebView，安装包小、内存占用低）或 Electron（生态成熟、开发快）
- 提供托盘图标、开机自启、原生通知、系统级剪贴板读取

**通用**
- 前端：Vue 3 或 React + Vite（配 vite-plugin-pwa）
- 二维码：qrcode 库；大文件分块：流式 + `Range` 断点续传
- 协议参考：**LocalSend Protocol v2**（开源且有完整协议文档，多设备互操作可参考）

---

## 7. 现有开源项目（建议先试用再决定自研）

| 项目 | 形态 | 与需求的差距 |
|---|---|---|
| **LocalSend** | Flutter 全平台（含 iOS） | 最接近目标；但"剪贴板/链接即时同步"体验弱，且每端都要装 App |
| **PairDrop**（Snapdrop 继承者） | 纯网页、局域网免安装 | 无自托管持久化、无剪贴板同步 |
| **KDE Connect** | 全平台客户端 | 功能最全（剪贴板/通知同步），iOS 端支持一般 |
| **Syncthing** | 文件夹自动同步 | 思路不同（同步而非点对点发送） |

**建议：先装 LocalSend 用一周。** 它满足的部分不用做；不满足的点（很可能就是"发链接到手机更顺手"、"不用装 App"、"传输历史"），就是你这个项目差异化的需求清单。

---

## 8. 分期路线图与工作量估算

| 版本 | 内容 | 工作量 |
|---|---|---|
| **V0.1** | PC 起 HTTP 服务 + 二维码 + 网页上传/下载 | 1–2 天 → 直接解决"图片手机↔电脑" |
| **V0.2** | WebSocket 文本通道 + 双向剪贴板按钮 + 传输历史 | 2–3 天 → 解决"链接电脑→手机" |
| **V0.3** | PWA 化、多设备管理、自动接收、Windows 通知、托盘程序 | 约 1 周 |
| **V0.4** | Tailscale/VPS 跨网络、HTTPS + E2E 加密、右键菜单、iOS 快捷指令 | 1–2 周 |

完整形态约 **2–4 周业余时间**；V0.1 + V0.2 两个周末就能覆盖你描述的两个核心痛点。

---

## 9. 总结

- 可行，且体量可控；核心架构一句话：**PC 常驻一个带网页的服务，手机/iPad 扫码即用**
- 最大的变量是 iPhone/iPad 的系统沙盒 → 用 PWA + 快捷指令绕开原生开发，是本项目的关键决策
- 跨网络需求优先用 Tailscale 解决，不要一上来就写中继服务器
- 先跑 V0.1 验证"扫码—传图"核心链路，再逐步加剪贴板同步和跨网络
