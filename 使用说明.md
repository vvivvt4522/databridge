# 三端互通 · 使用说明

PC / Android / iPad 之间互传图片、文件和剪贴板内容，不经过微信 QQ，数据只在你的设备之间流动。

**v0.2 更新**
- 📱 手机端**自动连接**：打开 App 自动搜索并连上 PC；网络切换也会自动重连
- 🖼️ **在线预览**：点击图片/视频名称直接看大图、播放视频，不用先保存
- ⚡ **自动保存**：手机收到图片/视频自动存入相册（可关）
- 📷 **相册选择器**：发照片变成熟悉的网格多选，还能发任意文件
- 📦 修复：大文件/APK 传输失败（改为原生流式保存，任意大小都稳）
- 🔒 安全加固：配对失败限速拉黑、401 延迟应答
- ✅ **原图直传，从不压缩**：传输的是文件的原始字节，缩略图仅用于界面显示

---

## 架构一览

```
┌───────────────┐   WebSocket/HTTP    ┌──────────────────┐
│ Android App   │ ──────────────────▶ │ PC 端 (Electron)  │
│ (DataBridge.apk)│ ◀────────────────── │ 内置服务器 + 托盘   │
└───────────────┘                     └──────────────────┘
        ▲                                     ▲
        │            ┌─────────────────┐      │
        └─────────── │ iPad App (IPA)   │ ─────┘
                     └─────────────────┘
```

- PC 端是"服务器 + 应用"：双击启动后，手机/iPad 都连它
- 三端界面一致：**文件页**传图片/文件，**剪贴板页**同步文字和链接，**设备页**看在线设备
- PC 收到的文件保存在 `server\data\received` 目录，托盘可一键打开

---

## 一、启动 PC 端

1. 双击 **启动PC端.bat**
2. Windows 防火墙首次弹窗 → 勾选"专用网络" → 点 **允许访问**（重要！否则手机连不上）
3. 窗口顶部显示连接状态；切到"设备"页可以看到 **二维码和完整地址**

## 二、Android 手机（真 App）

1. 安装 **DataBridge.apk**（允许"未知来源"）
2. 打开 App → **自动搜索并连接** PC，无需任何操作
   - 万一没搜到（路由器开了 AP 隔离等）会自动弹出手动输入框，粘贴 PC"设备"页地址即可
3. 用法：
   - **发照片**：文件页 → "选照片/视频" → 网格里随便多选 → 发送
   - **发任意文件（如 APK）**："选任意文件"
   - **收图片/视频**：自动存入相册（"设备"页下方开关可关）
   - **发剪贴板**：剪贴板页 → "发送我的剪贴板"
   - **收文字/链接**：剪贴板页点"复制"
   - **看图/看视频**：文件列表点名称 → 在线预览，不用先保存

## 三、PC 端功能

| 功能 | 位置 |
|---|---|
| 推送当前剪贴板到所有设备 | 托盘右键菜单 / 全局快捷键 **Ctrl+Alt+V** |
| 收到文字自动进剪贴板 | 默认开启，托盘菜单可关 |
| 收到文件弹通知 | 默认开启，托盘菜单可关 |
| 打开接收文件夹 | 托盘菜单 |
| 开机自启 | 托盘菜单勾选 |

## 四、iPad

原生 App（源码 `ios-app/`）通过 GitHub 云构建 + AltStore 自签安装，完整步骤见 **《iPad安装指南.md》**。
流程未走完之前，iPad 用 Safari 打开 PC"设备"页地址即可获得完整功能。

## 五、安全模型

- **配对码**：128 位随机数，首次配对后存在各设备本地，之后不再出现在输入框/地址栏
- **访问控制**：所有 API 都校验配对码，无配对码的设备只能看到空白页面
- **防暴力破解**：同一 IP 连续失败 8 次自动拉黑 10 分钟；每次失败延迟 0.6 秒应答
- **数据流向**：全部数据只经 PC 中转存本地，不经任何云/第三方
- **当前边界**：局域网内是 HTTP 明文 + 配对码模型，适合家庭/可信 WiFi；**不要在公共 WiFi 使用**。点对点 TLS 加密（自签名证书双向校验）在后续版本提供

## 六、常见问题

**手机搜不到 PC？**
- 检查是否同一 WiFi（路由器开了"AP 隔离"会搜不到）
- PC 防火墙是否放行
- 兜底：等 App 弹出手动输入框，粘贴 PC 端地址

**传 APK/大视频失败（v0.1 的问题）？**
- v0.2 已改为原生流式保存，若你还在用旧 APK 请先更新

**iPad 提示"无法验证 App"？**
- 设置 → 通用 → VPN与设备管理 → 信任开发者证书

**端口被占用？**
- 默认端口 8322，改 `server\data\config.json` 里的 `port`

## 七、目录说明

```
三端数据互通工具\
├── 启动PC端.bat          双击启动 PC 端
├── DataBridge.apk        安卓安装包（构建产物）
├── server\               服务器（Node.js）
│   └── data\received\    接收的文件
├── web\                  三端共用界面
├── pc\                   Electron 壳
├── android-app\          安卓源码 + build.bat（构建 APK）
├── ios-app\              iPad 源码（GitHub Actions 云构建）
├── iPad安装指南.md        iPad App 安装完整步骤
├── tools\安装环境.bat     换机/重装时一键部署环境
└── .github\workflows\    iPad App 云构建流水线
```
