# 三端互通 · 使用说明

PC / Android / iPad 之间互传图片、文件和剪贴板内容，不经过微信 QQ，数据只在你的设备之间流动。

## 架构一览

```
┌───────────────┐   WebSocket/HTTP    ┌─────────────────┐
│ Android App   │ ──────────────────▶ │   PC 端 (Electron) │
│ (DataBridge.apk) │ ◀────────────────── │ 内置服务器 + 托盘    │
└───────────────┘                     └─────────────────┘
        ▲                                     ▲
        │            ┌─────────────────┐      │
        └─────────── │ iPad App (IPA)   │ ─────┘
                     └─────────────────┘
```

- PC 端是"服务器 + 应用"：双击启动后，手机/iPad 都连它
- 三端界面一致：**文件页**传图片/文件，**剪贴板页**同步文字和链接，**设备页**看在线设备
- PC 收到的文件保存在 `server\data\received` 目录，托盘可一键打开

---

## 一、启动 PC 端

1. 双击 **启动PC端.bat**
2. Windows 防火墙首次弹窗 → 勾选"专用网络" → 点 **允许访问**（重要！否则手机连不上）
3. 窗口顶部显示连接状态；切到"设备"页可以看到 **二维码和完整地址**

> 如果换了 WiFi / IP 变了，PC 端"设备"页里的地址会自动更新，手机端重新配一次即可。

## 二、连接 Android 手机（真 App）

1. 把项目根目录的 **DataBridge.apk** 传到手机（数据线、或用本工具装好前先随便用一次浏览器开 `http://电脑IP:8322`）
2. 安装时允许"来自未知来源的应用"
3. 打开 App → 自动搜索同一 WiFi 下的 PC → 点你的电脑名称
   - 搜不到就用"手动输入地址"，粘贴 PC 端"设备"页显示的地址
4. 顶部显示"已连接"即可互传

**手机端用法**
- 发图片：文件页 → 点上传区选图 → PC 自动保存并弹通知
- 收图片：文件页点"保存" → 直接进系统相册
- 发剪贴板：剪贴板页 → "发送我的剪贴板"（或长按输入框粘贴后发送）
- 收文字/链接：剪贴板页点"复制" → 已进剪贴板，去任何 App 粘贴

## 三、连接 iPad（真 App，AltStore 自签）

iPad 端是原生 App（源码在 `ios-app/`），但苹果规定必须签名后才能装。免费方案流程：

### 准备（一次性）
1. **注册 GitHub 账号**（没有的话 github.com 免费注册）
2. 把整个项目文件夹推送到你的 GitHub 仓库：
   - 项目已含 `.github/workflows/build-ios.yml`，推送后自动在云端 Mac 上构建
3. **下载 AltServer**：https://altstore.io（Windows 版），安装并启动（任务栏常驻）
4. iPad 上装 AltStore：
   - 电脑 iTunes + iCloud（官网版，不是商店版）装好并登录你的 Apple ID
   - iPad 连数据线到电脑 → 任务栏 AltServer 图标 → Install AltStore → 选你的 iPad
   - iPad 设置 → 通用 → VPN与设备管理 → 信任你的 Apple ID

### 构建并安装 App
1. GitHub 仓库 → Actions 页 → 等 "Build iOS IPA" 跑完（绿勾）
2. 下载 Artifact：**DataBridge-unsigned-ipa**（未签名 IPA）
3. 解压得到 `DataBridge-unsigned.ipa`
4. 任务栏 AltServer → **Sideload .ipa**（AltStore 2.x）→ 选这个 ipa → 输入 Apple ID
5. iPad 的 AltStore → My Apps → 等待安装完成

### 注意
- 免费 Apple ID 签名的 App **7 天过期**：过期前让电脑 AltServer 开着，iPad 和电脑同一 WiFi，打开 AltStore 点刷新即可续期
- 免费 Apple ID 最多同时 3 个自签 App
- 明天测试如果 AltStore 流程没就绪，可先用 iPad 的 **Safari 打开 PC 端"设备"页显示的地址**，功能完全一样，不影响测试互通

---

## 四、PC 端功能

| 功能 | 位置 |
|---|---|
| 推送当前剪贴板到所有设备 | 托盘右键菜单 / 全局快捷键 **Ctrl+Alt+V** |
| 收到文字自动进剪贴板 | 默认开启，托盘菜单可关 |
| 收到文件弹通知 | 默认开启，托盘菜单可关 |
| 打开接收文件夹 | 托盘菜单 |
| 开机自启 | 托盘菜单勾选 |

## 五、常见问题

**手机搜不到 PC？**
- 检查是否同一 WiFi（路由器开了"AP 隔离"的话会搜不到）
- PC 防火墙是否放行（弹窗时点允许；错过弹窗就去 防火墙设置 里允许 node/electron）
- 兜底：手机 App 选"手动输入地址"，粘贴 PC 端显示的地址

**连接时好时坏？**
- 电脑休眠会断开；建议电源设置里关闭"休眠"（睡眠时网络会断）

**iPad 提示"无法验证 App"？**
- 设置 → 通用 → VPN与设备管理 → 信任你的开发者证书

**端口被占用？**
- 默认端口 8322，改 `server\data\config.json` 里的 `port`

## 六、目录说明

```
三端数据互通工具\
├── 启动PC端.bat          双击启动 PC 端
├── DataBridge.apk        安卓安装包（构建产物）
├── server\               服务器（Node.js）
│   └── data\received\    接收的文件
├── web\                  三端共用界面
├── pc\                   Electron 壳
├── android-app\          安卓源码 + build.bat（构建 APK）
├── ios-app\              iPad 源码（GitHub Actions 云端构建）
├── tools\安装环境.bat     换机/重装时一键部署环境
└── .github\workflows\    iPad App 云构建流水线
```

## 七、安全边界

- 当前为局域网 HTTP + 配对码模式：**仅限家庭/可信 WiFi 使用**
- 后续版本计划：HTTPS 加密、跨网络中继（Tailscale/VPS）、E2E 加密
